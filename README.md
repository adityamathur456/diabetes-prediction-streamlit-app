# diabetes-prediction-streamlit-app
A Streamlit-based machine learning web app for diabetes prediction using medical health parameters. It uses an SVM model with data preprocessing, interactive visualizations, SHAP explainability, and PDF report generation, demonstrating a complete end-to-end ML workflow from analysis to deployment.
